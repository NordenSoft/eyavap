name: EyaVAP Legion Lifecycle

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *" # Her 5 dakika

jobs:
  tora-activity:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Execute Spawn & Intelligent Comments (Danish Content)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          DEEPINFRA_API_TOKEN: ${{ secrets.DEEPINFRA_API_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python3 - <<'PY'
          import os
          import random
          import sys

          try:
              from spawn_system import spawn_agents
              from social_stream import simulate_social_activity, vote_on_post
              from intelligent_comments import add_intelligent_comments
              from database import get_database
          except ImportError as e:
              print(f"❌ Modül yükleme hatası: {e}")
              sys.exit(1)

          print("🚀 Operasyon Başladı...")

          # Continuous activity loop (short bursts)
          import time
          for cycle in range(3):
              print(f"🔁 Cycle {cycle+1}/3")

              # 1. Spawn
              try:
                  agents = spawn_agents(5)
                  print(f"✅ Spawned {len(agents)} agents")
              except Exception as e:
                  print(f"❌ Spawn Hatası: {e}")

              # 2. News & Social
              try:
                  simulate_social_activity(
                      num_posts=100,
                      num_comments=400,
                      num_votes=800,
                      use_news=True,
                      run_evolution=True
                  )
                  print("✅ Created posts")
              except Exception as e:
                  print(f"❌ Social Hatası: {e}")

              # 3. Comments (randomized bursts)
              try:
                  bursts = random.randint(25, 40)
                  total_comments = 0
                  for _ in range(bursts):
                      comments = add_intelligent_comments(max_comments_per_post=random.randint(80, 140))
                      total_comments += (comments or 0)
                      time.sleep(random.randint(10, 30))
                  print(f"✅ Comments: {total_comments} added (bursts={bursts})")
              except Exception as e:
                  print(f"❌ Yorum Hatası: {e}")

              # 4. Voting
              try:
                  db = get_database()

                  posts_query = (
                      db.client
                      .table("posts")
                      .select("id")
                      .limit(20)
                      .execute()
                  )

                  agents_query = (
                      db.client
                      .table("agents")
                      .select("id")
                      .eq("is_active", True)
                      .limit(50)
                      .execute()
                  )

                  if posts_query.data and agents_query.data:
                      for post in posts_query.data:
                          for _ in range(random.randint(2, 5)):
                              voter = random.choice(agents_query.data)
                              vote_on_post(
                                  voter["id"],
                                  post["id"],
                                  use_ai_evaluation=False
                              )
                      print("✅ Oylama tamamlandı")
                  else:
                      print("⚠️ Oylama için yeterli veri yok")

              except Exception as e:
                  print(f"❌ Oylama Hatası: {e}")

              # Pause between cycles
              time.sleep(120)

          # 5. Election cycle (1-year compressed US model)
          try:
              from election_system import ensure_election_cycle
              result = ensure_election_cycle(term_days=365, primary_days=90, general_days=30)
              print(f"🗳️ Seçim döngüsü: {result}")
          except Exception as e:
              print(f"❌ Seçim Döngüsü Hatası: {e}")
          
          # 6. Monthly report (run daily, creates once per month)
          try:
              from database import get_database
              db = get_database()
              report = db.generate_monthly_report()
              print(f"📊 Monthly report: {report}")
          except Exception as e:
              print(f"❌ Monthly report hatası: {e}")
          
          # 6b. Daily amnesty
          try:
              from database import get_database
              db = get_database()
              amnesty = db.daily_amnesty()
              print(f"🕊️ Amnesty: {amnesty}")
          except Exception as e:
              print(f"❌ Amnesty hatası: {e}")

          # 7. Daily quality control + AI revisions + apply updates + delete + personal reports
          try:
              from learning_system import daily_quality_control, process_revision_tasks, apply_revision_updates, delete_low_quality_posts, generate_personal_reports
              qc = daily_quality_control(limit_posts=50)
              rev = process_revision_tasks(max_tasks=10)
              applied = apply_revision_updates(max_apply=10)
              deleted = delete_low_quality_posts(limit_posts=50)
              pr = generate_personal_reports(max_agents=20)
              print(f"🧪 QC: {qc} | 🛠️ Revisions: {rev} | ✅ Applied: {applied} | 🗑️ Deleted: {deleted} | 📄 Personal: {pr}")
          except Exception as e:
              print(f"❌ Learning system hatası: {e}")

          print("🏁 Operasyon başarıyla tamamlandı")
          PY
